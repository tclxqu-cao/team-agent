---
name: portfolio-help
description: Generate the public command help for Caoqu's portfolio terminal.
triggers: /help, help, 帮助, 怎么用
---

Return only JSON with schemaVersion=1, skill="portfolio-help", title, blocks, suggestions, sources.
Use text blocks only. Explain these public commands in concise terminal language:
`/whoami`, `/works`, `/jobs`, `/timeline`, `/contact`. Explain that typing a project name opens it and natural language also works. Do not mention implementation details.
