- 应用层：通过 `RunAgentUseCase` 和 `ManageSessionUseCase` 编排领域逻辑，依赖注入领域接口（如 `IAgentFactory`、`ISessionStore`）。
- 基础设施层：提供统一的 `SQLiteDatabase` 单例管理，封装复杂的 Schema 迁移逻辑；并通过一系列 `SQLite*Store` 类（如 `SQLiteAgentStore`）实现具体的数据访问对象（DAO），将领域实体映射为关系型数据。
- 依赖方向：严格遵循依赖倒置原则，应用层仅依赖抽象接口，基础设施层负责具体实现。