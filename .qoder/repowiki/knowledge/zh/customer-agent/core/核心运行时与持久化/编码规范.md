- 基础设施层的 Store 类统一采用 `SQLite[Entity]Store` 命名规范，并实现对应的 `I[Entity]Store` 领域接口。
- 所有 Store 实现均通过构造函数接收 `baseDir` 参数，并利用 `getDatabase(baseDir)` 获取共享的数据库实例。
- 在 `SQLiteDatabase.ts` 中集中管理所有表的 DDL 定义及增量迁移逻辑（如 `migrateSessionsProjectIdNullable`），确保 Schema 演进的一致性。