- 采用接口隔离设计，通过 `entities.ts` 定义 `IMemoryStore` 契约及核心数据模型（`MemoryEntry`, `MemoryType`）。
- `FileSystemMemoryStore` 作为唯一实现，利用 Node.js `fs/promises` 将记忆条目映射为独立 Markdown 文件，并维护 `MEMORY.md` 索引。
- 内部封装了 YAML Frontmatter 的解析与生成逻辑，支持按名称、类型和内容的加权搜索算法。
- 模块入口 `index.ts` 仅暴露类型定义与接口，隐藏具体存储实现细节。