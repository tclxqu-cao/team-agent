- 入口文件 `index.ts` 统一导出 `MCPClient`、`MCPManager` 及领域实体，确立模块公共 API 边界。
- `MCPManager` 作为核心协调层，负责客户端实例的创建、缓存与生命周期调度。
- `MCPClient` 封装底层通信逻辑，处理具体的 MCP 协议交互。
- `entities.ts` 定义领域数据模型，被客户端与管理器共同依赖，确保数据结构一致性。