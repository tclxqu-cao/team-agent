- 所有文件路径在传递给 LSP 协议前均通过 `pathToFileURL` 转换为 URI 格式。
- JSON-RPC 请求采用自增 ID 机制，并通过 `Map` 维护 pending 状态以匹配异步响应。
- 诊断信息获取采用降级策略：优先尝试 LSP 3.17+ 的 pull-based `textDocument/diagnostic`，失败则回退至 push-based 事件监听。