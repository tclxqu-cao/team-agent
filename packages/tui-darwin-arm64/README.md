# AgentRoam TUI for macOS arm64

This optional platform package supplies the `agent-tui` command installed with `agentroam@preview`.
It is version-locked to the matching AgentRoam preview release and runs on Node.js 22 without requiring Bun.
