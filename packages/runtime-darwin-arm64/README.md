# AgentRoam runtime for macOS arm64

Platform runtime installed automatically by `agentroam`. It contains the Next.js server, Web console, SQLite, and PTY native files for Node.js 22 on Apple Silicon.
