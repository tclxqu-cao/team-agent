import{mkdir}from"node:fs/promises";import{resolve,delimiter}from"node:path";import{fileURLToPath}from"node:url";import type{ChildProcess}from"node:child_process";import{findAvailablePort}from"./port.js";import{ProcessSupervisor}from"./process-supervisor.js";import type{CliOptions}from"./args.js";import type{PairingSecret}from"./pairing.js";import{repairNativeRuntimePermissions}from"./native-runtime.js";
export interface RuntimeHandle{port:number;localUrl:string;dataDir:string;needsSetup:boolean;close():Promise<void>;exited:Promise<{code:number|null;signal:NodeJS.Signals|null}>;process:ChildProcess;}
export class RuntimeManager{
 constructor(private supervisor=new ProcessSupervisor()){}
 async start(options:CliOptions,pairing?:PairingSecret):Promise<RuntimeHandle>{const port=await findAvailablePort(options.port);const dataDir=resolve(options.dataDir);await Promise.all(["data","bin","cache","logs"].map(name=>mkdir(resolve(dataDir,name),{recursive:true})));const runtimeRoot=fileURLToPath(new URL("../runtime/",import.meta.url));await repairNativeRuntimePermissions(runtimeRoot);const gateway=resolve(runtimeRoot,"ws-server.mjs");const child=this.supervisor.spawn(process.execPath,[gateway],{cwd:runtimeRoot,env:{...process.env,NODE_ENV:"production",NEXT_DIST_DIR:".next",PORT:String(port),HOST:"127.0.0.1",AGENT_DATA_DIR:resolve(dataDir,"data"),AGENT_WEB_ROOTS:options.roots.join(delimiter),AGENT_TRUST_TUNNEL_PROXY:"1",AGENT_PAIRING_HASH:pairing?.hashHex||"",AGENT_PAIRING_EXPIRES_AT:pairing?.expiresAt||""}});const log=(stream:NodeJS.ReadableStream|null)=>stream?.on("data",chunk=>process.stderr.write(chunk));log(child.stdout);log(child.stderr);const exited=new Promise<{code:number|null;signal:NodeJS.Signals|null}>(resolve=>child.once("exit",(code,signal)=>resolve({code,signal})));const localUrl=`http://127.0.0.1:${port}`;const status=await waitForHealth(`${localUrl}/api/web-auth/status`,child,20000);return{port,localUrl,dataDir,needsSetup:!!status.needsSetup,process:child,exited,close:()=>this.supervisor.stopAll()};}
}
async function waitForHealth(url:string,child:ChildProcess,timeoutMs:number):Promise<any>{const end=Date.now()+timeoutMs;while(Date.now()<end){if(child.exitCode!==null)throw new Error(`local server exited with ${child.exitCode}`);try{const response=await fetch(url);if(response.ok)return response.json();}catch{}await new Promise(resolve=>setTimeout(resolve,250));}throw new Error("local server health check timed out");}
