import{describe,expect,it}from"vitest";import{detectPlatform}from"./platform.js";
describe("detectPlatform",()=>{it("maps supported targets",()=>{expect(detectPlatform("darwin","arm64","22.1.0")).toBe("darwin-arm64");expect(detectPlatform("win32","x64","22.1.0")).toBe("windows-amd64");});it("rejects unsupported versions",()=>expect(()=>detectPlatform("darwin","arm64","23.0.0")).toThrow("Node.js 22"));});
