{
  "name": "@agent/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "AgentRoam Android/iOS 壳：Capacitor 打包 packages/webapp 构建产物，通过可配置的远端服务器地址使用 Agent 能力",
  "scripts": {
    "sync": "cd ../webapp && bun run build && cd ../mobile && cap sync",
    "sync:web": "cap copy",
    "update": "cap update android && cap update ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android:debug": "cd android && ./gradlew assembleDebug",
    "build:android:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
